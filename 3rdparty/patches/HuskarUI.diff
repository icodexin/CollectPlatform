diff --git a/src/imports/HusMenu.qml b/src/imports/HusMenu.qml
index 6dba48a..95087cd 100644
--- a/src/imports/HusMenu.qml
+++ b/src/imports/HusMenu.qml
@@ -289,6 +289,11 @@ Item {
         __listView.model = [];
     }
 
+    function clearSelectedMenu() {
+        __private.gotoMenuKey = '';
+        __private.selectedItem = null;
+    }
+
     component MenuButton: HusButton {
         id: __menuButtonImpl
 
diff --git a/src/imports/HusScrollBar.qml b/src/imports/HusScrollBar.qml
index e12d098..2ab2386 100644
--- a/src/imports/HusScrollBar.qml
+++ b/src/imports/HusScrollBar.qml
@@ -16,14 +16,12 @@ T.ScrollBar {
     property string contentDescription: ''
 
     objectName: '__HusScrollBar__'
-    width: control.orientation == Qt.Vertical ? 10 : parent.width
-    height: control.orientation == Qt.Horizontal ? 10 : parent.height
-    anchors.right: control.orientation == Qt.Vertical ? parent.right : undefined
-    anchors.bottom: control.orientation == Qt.Horizontal ? parent.bottom : undefined
-    leftPadding: control.orientation == Qt.Horizontal ? (leftInset + 10) : leftInset
-    rightPadding: control.orientation == Qt.Horizontal ? (rightInset + 10) : rightInset
-    topPadding: control.orientation == Qt.Vertical ? (topInset + 10) : topInset
-    bottomPadding: control.orientation == Qt.Vertical ? (bottomInset + 10) : bottomInset
+    implicitWidth: Math.max(implicitBackgroundWidth + leftInset + rightInset,
+        implicitContentWidth + leftPadding + rightPadding)
+    implicitHeight: Math.max(implicitBackgroundHeight + topInset + bottomInset,
+        implicitContentHeight + topPadding + bottomPadding)
+    verticalPadding: vertical ? 12 : 3
+    horizontalPadding: horizontal ? 12 : 3
     policy: T.ScrollBar.AlwaysOn
     minimumSize: {
         if (control.orientation == Qt.Vertical)
@@ -33,54 +31,23 @@ T.ScrollBar {
     }
     visible: (control.policy != T.ScrollBar.AlwaysOff) && control.size !== 1
     contentItem: Item {
+        implicitWidth: control.interactive ? __private.maxLine : __private.minLine
+        implicitHeight: control.interactive ? __private.maxLine : __private.minLine
+
         Rectangle {
-            width: {
-                if (control.orientation == Qt.Vertical) {
-                    return __private.visible ? 6 : 2;
-                } else {
-                    return parent.width;
-                }
-            }
-            height: {
-                if (control.orientation == Qt.Vertical) {
-                    return parent.height;
-                } else {
-                    return __private.visible ? 6 : 2;
-                }
-            }
+            id: bar_rect
+            width: control.vertical ? __private.minLine : parent.width
+            height: control.horizontal ? __private.minLine : parent.height
+            radius: control.vertical ? width * 0.5 : height * 0.5
+            color: control.colorBar
             anchors.verticalCenter: parent.verticalCenter
             anchors.horizontalCenter: parent.horizontalCenter
-            radius: control.orientation == Qt.Vertical ? width * 0.5 : height * 0.5
-            color: control.colorBar
-            opacity: {
-                if (control.policy == T.ScrollBar.AlwaysOn) {
-                    return 1;
-                } else if (control.policy == T.ScrollBar.AsNeeded) {
-                    return __private.visible ? 1 : 0;
-                } else {
-                    return 0;
-                }
-            }
-
-            Behavior on width { enabled: control.animationEnabled; NumberAnimation { duration: HusTheme.Primary.durationFast } }
-            Behavior on height { enabled: control.animationEnabled; NumberAnimation { duration: HusTheme.Primary.durationFast } }
-            Behavior on opacity { enabled: control.animationEnabled; NumberAnimation { duration: HusTheme.Primary.durationFast } }
         }
     }
     background: Rectangle {
         color: control.colorBg
-        opacity: __private.visible ? 1 : 0
-
-        Behavior on opacity { enabled: control.animationEnabled; NumberAnimation { duration: HusTheme.Primary.durationFast } }
-    }
-
-    onHoveredChanged: {
-        if (hovered) {
-            __exitTimer.stop();
-            __private.exit = false;
-        } else {
-            __exitTimer.restart();
-        }
+        radius: control.vertical ? width * 0.5 : height * 0.5
+        opacity: 0
     }
 
     component HoverIcon: HusIconText {
@@ -88,9 +55,7 @@ T.ScrollBar {
         property bool hovered: false
 
         colorIcon: hovered ? HusTheme.HusScrollBar.colorIconHover : HusTheme.HusScrollBar.colorIcon
-        opacity: __private.visible ? 1 : 0
-
-        Behavior on opacity { enabled: control.animationEnabled; NumberAnimation { duration: HusTheme.Primary.durationFast } }
+        opacity: control.background.opacity
 
         MouseArea {
             anchors.fill: parent
@@ -103,15 +68,80 @@ T.ScrollBar {
 
     QtObject {
         id: __private
-        property bool visible: control.hovered || control.pressed || !exit
-        property bool exit: true
+        property int maxLine: 8
+        property int minLine: 3
     }
 
-    Timer {
-        id: __exitTimer
-        interval: 800
-        onTriggered: __private.exit = true;
-    }
+    states: [
+        State {
+            name: "expanded"
+            when: control.hovered || control.pressed
+            PropertyChanges {
+                bar_rect.width: vertical ? __private.maxLine : contentItem.width
+                bar_rect.height: horizontal ? __private.maxLine : contentItem.height
+                control.background.opacity: 1
+            }
+        },
+        State {
+            name: "hide"
+            when: (control.policy === T.ScrollBar.AsNeeded) && !control.active
+            PropertyChanges {
+                control.opacity: 0
+            }
+        }
+    ]
+
+    transitions: [
+        Transition {
+            to: ""
+            SequentialAnimation {
+                PauseAnimation { duration: control.animationEnabled ? HusTheme.Primary.durationSlow * 2 : 0 }
+                ParallelAnimation {
+                    NumberAnimation {
+                        target: bar_rect
+                        properties: control.vertical ? "width" : "height"
+                        duration: control.animationEnabled ? HusTheme.Primary.durationFast : 0
+                        easing.type: Easing.OutCubic
+                    }
+                    NumberAnimation {
+                        target: control.background
+                        properties: "opacity"
+                        duration: control.animationEnabled ? HusTheme.Primary.durationFast : 0
+                    }
+                }
+            }
+        },
+        Transition {
+            to: "expanded"
+            SequentialAnimation {
+                PauseAnimation { duration: control.animationEnabled ? HusTheme.Primary.durationFast : 0 }
+                ParallelAnimation {
+                    NumberAnimation {
+                        target: bar_rect
+                        properties: control.vertical ? "width" : "height"
+                        duration: control.animationEnabled ? HusTheme.Primary.durationFast : 0
+                        easing.type: Easing.OutCubic
+                    }
+                    NumberAnimation {
+                        target: control.background
+                        properties: "opacity"
+                        duration: control.animationEnabled ? HusTheme.Primary.durationFast : 0
+                    }
+                }
+            }
+        },
+        Transition {
+            to: "hide"
+            SequentialAnimation {
+                PauseAnimation { duration: control.animationEnabled ? HusTheme.Primary.durationSlow : 0 }
+                NumberAnimation {
+                    target: control
+                    properties: "opacity"
+                    duration: control.animationEnabled ? HusTheme.Primary.durationSlow : 0
+                }
+            }
+        }
+    ]
 
     Loader {
         active: control.orientation == Qt.Vertical
